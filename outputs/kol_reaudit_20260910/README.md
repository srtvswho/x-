# 八位作者复评 · 2026-09-10

结论见 report.md。主表采用 strict21；statistics.json.gz 含所有口径的统计，scored_events.json.gz 含 6,418 条原始方向记录、完整帖子正文、语义及证券身份判定、入场价格和成熟窗口收益。所有回报为小数。

用 Python 标准库离线重算统计（无需模型或网络）：

```sh
python scripts/reaudit_kols_20260910.py summarize
```

新的历史节点回放见 ../focus_backtest_20260910。完整日线价格缓存保存在本次审计附件中，未重复放入此公开目录。若要重新计算价格层，需要该价格附件或自行重新取价；本目录的 summarize 只重算冻结收益上的聚合统计。prep/calculate 是价格层重算步骤，不能在没有价格缓存时当作离线统计命令。人工判定依赖冻结 event_index，不可重排。

这些记录包含剔除项，并不都是合格交易；应先过滤语义、身份、市场和价格条件，再做时间去重。旧作者字母配置未直接覆盖，专项证据供重点关注页使用。用户已明确授权本次公开发布。
